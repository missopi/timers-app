import { StyleSheet } from "react-native";

export default StyleSheet.create({ 
 trafficContainer: {
    flex: 1,
  },
  trafficText: {
    fontSize: 80,
    fontWeight: 'bold',
    textAlign: 'center',
  },
})
